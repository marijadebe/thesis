\iffalse
\clearpage
\section{Monoids and acts}
\subsection{Preliminaries}
    \begin{definition}
        A 3-tuple $\mathcal{S} = (S,\star,e)$ where $S$ is a set and $\star : S\times S \to S$, $e : 1 \to S$ are 
        maps is called a \textbf{monoid}, if the following two conditions hold
        \begin{enumerate}
            \item $\forall x,y,z\in S : (x\star y)\star z = x\star(y\star z)$
            \item $\forall x\in S : x\star e = x = e \star x$
        \end{enumerate}
    \end{definition}
    \begin{example}
        Let $X$ be a nonempty set. Denote by $T(X)$ the set of all maps
        $X\to X$.\\ Then $\mathcal{T}(X) = (T(X),\circ,\text{id}_X) $, where $\circ$ is a 
        composition of maps and $\text{id}_X$ the identity map, is a monoid. We will
        call $\mathcal{T}(X)$ the transformation monoid of $X$.\par
        Let $\mathcal{S} = (S,\star,e)$ be a monoid, we define the opposite monoid $\opp{\mathcal{S}}=(S,\star_{\text{op}}, e)$ where
        \[\forall x,y\in S: x\star_{\text{op}} y = y\star x\]
    \end{example}
    \begin{definition}
        Let $\mathcal{S}$ be a monoid and $X$ a nonempty set. A tuple $(X,\mu)$, where $\mu : S\times X \to X$ is called 
        a \textbf{left $\mathcal{S}$-act} if the map $\mu$ satisfies the following
        \begin{enumerate}
            \item $\forall r,s \in S\,\forall x \in X : \mu(rs, x) = \mu(r,\mu(s,x))$
            \item $\forall x \in S : \mu(e,x) = x$
        \end{enumerate}
        we will denote a left $\mathcal{S}$-act as ${}_{\mathcal{S}}X$. Similarly we
        define a right $\mathcal{S}$-act and use the notation $X_\mathcal{S}$.
    \end{definition}
    \begin{remark}
        When we use the word $\mathcal{S}$-act we will mean the left $\mathcal{S}$-act, unless 
        stated otherwise.
    \end{remark}
    \begin{definition}
        Given $\act{S}{X}$ and $\act{S}{Y}$ we call $f: X \to Y$ an
        \textbf{act homomorphism}, if 
        \[
            \forall s\in S\,\forall x \in X : f(s\cdot x) = s\cdot f(x)
        \]
    \end{definition}
    \begin{proposition}
        Acts with their homomorphisms form a category, which we will denote $\cat{$\mathcal{S}$-Act}$.
    \end{proposition}
    \begin{construction}[Powerset Act Monad (TBA)]
        Consider an act $\act{S}{A}$, then we have an endofunctor
        $\mathcal{P}$ in $\cat{$\mathcal{S}$-Act}$. 
        \[\act{S}{A} \mapsto \act{S}{P(A)}\]
        given 
    \end{construction}
    \begin{definition}
        Let $M$ and $N$ be monoids, $f: M \to N$, then we have a functor $\Gamma_f : \actcat{M} \to \actcat{N}$.
    \end{definition}

    Both $\actcat{S}$ and $\actcat{S}_0$ are monoidal categories 
    with the tensor product being a direct product of acts. 
    The unit being the terminal and zero objects respectively.\par 
        A monoid in $\actcat{S}$ is an act equipped with 
        a multiplication map $\circ$ and a unit $e$ such that
        the following holds
        \[
            (x\circ y)\circ z = x\circ (y\circ z)
        \]
        \[
            e \circ x = x = x \circ e
        \]
        \[
            s\cdot (x\circ y) = (s\cdot x) \circ (s\cdot y)
        \]
        \[
            s\cdot e = e
        \]
        Act homomorphism $f$ is a homomorphism of monoids if
        \[
            f(x\circ y) = f(x)\circ f(y)
        \]
        \[
            f(e) = e
        \]
\subsection{Free objects}
    \begin{definition}
        Let $X$ be a set and $\mathcal{S}$ a monoid. We define the \textbf{free $\mathcal{S}$-act on X} to be 
        the tuple  
        \[(S\times X,\cdot : S\times (S\times X) \to S\times X)\]
        where $s_1\cdot (s,x) = (s_1s,x)$.
    \end{definition}
    It is an act, since $e\cdot(s,x) = (es,x) = (s,x)$ and $(s_1s_2)\cdot(s,x) = 
    ((s_1s_2)s,x) = (s_1(s_2s),x) = s_1\cdot(s_2s,x) = s_1\cdot(s_2\cdot(s,x))$.
    \begin{definition}
        In $\actcat{S}_0$ we define the free act on $X$ by taking the quotient of 
        $S\times X$ by an equivalence relation $\sim$, where 
        \[
            (s,x)\sim (r,y) \iff (s=r \land x=y) \lor s=0=r
        \]
        (it is easy to see that it is really an equivalence relation)
        and putting for $[(s,x)]_\sim\in S\times X/\sim$ and $r\in\mathcal{S}$
        \[
            r\cdot[(s,x)]_\sim = [(rs,x)]_\sim
        \]    
    \end{definition}
    \begin{remark}
        It is a well defined act. Take $(s,x)\sim(r,y)$
    \end{remark}
\fi
\subsection{Localisation of a pointed monoids and acts}
\begin{construction}
    Let $(S,\cdot,0,1)$ be a pointed commutative monoid and 
    $K\subseteq S$ a submonoid of $S$ such that $0\not\in K$. 
    We define $K^{-1}S = S\times K/\sim$, where 
    \[
        (s_1,k_1)\sim(s_2,k_2)\iff \exists v\in K : s_1k_2v = s_2k_1v 
    \]
    then $K^{-1}S$ is well-defined and admits the structure of a pointed commutative monoid.
\end{construction}
\begin{proof}[Proof]
    Relation $\sim$ is reflexive, since $1\in K$. It is clearly symmetric. 
    Let $(s_1,k_1)\sim(s_2,k_2)\sim(s_3,k_3)$, so there are $v,w\in K$ such that 
    $s_1k_2v = s_2k_1v$ and $s_2k_3w = s_3k_2w$, then 
    \[
    (s_1k_3)k_2vw = s_1k_2vk_3w = s_2k_1vk_3w = s_2k_3wk_1v = s_3k_2wk_1v = (s_3k_1)k_2vw
    \]
    therefore $(s_1,k_1)\sim(s_3,k_3)$. We will denote the equivalence classes as
    $\frac{s}{k} = [s,k]_{\sim}$. Define 
    \[
        \frac{s_1}{k_1}\cdot\frac{s_2}{k_2} = \frac{s_1 s_2}{k_1 k_2}
    \]
    to prove well-definedness assume $(s_1,k_1)\sim(d_1,l_1)$ and $(s_2,k_2)\sim(d_2,l_2)$, then 
    we find $u,v\in K$ such that 
    $s_1l_1u=d_1k_1v$ and $s_2l_2v = d_2k_2v$, then 
    \[
    s_1s_2l_1l_2uv = (s_1l_1u)(s_2l_2)v = d_1k_1ud_2k_2v = d_1d_2k_1k_2uv 
    \]
    thus
    \[
        \frac{s_1}{k_1}\cdot\frac{s_2}{k_2} = \frac{s_1s_2}{k_1k_2} = \frac{d_1d_2}{l_1l_2} = \frac{d_1}{l_1}\cdot\frac{d_2}{l_2}
    \]
    We define the unit $1 = \frac{1}{1}$ and $0 = \frac{0}{1}$. The associativity, commutativity and unit axioms are clearly 
    satisfied. We have 
    \[
        0\cdot\frac{s}{k} = \frac{0}{k} = \frac{0}{1} = 0
    \]
    since $(0,k)\sim(0,1)$, because $0\cdot 1 = 0 = 0\cdot k$. 
\end{proof}
\begin{definition}
    We define the \textbf{localisation map} $\Lambda: S \to K^{-1}S$ by $\Lambda(x) = \frac{x}{1}$. It is a 
    homomorphism of pointed monoids.  
\end{definition}
\begin{construction}
    Let $K\subseteq S$ be a submonoid of $S$ such that $0\not\in S$ and $A$ an $S$ act. We define 
    $K^{-1}A = A\times K/\sim$, where 
    \[
        (a_1,k_1)\sim(a_2,k_2) \iff \exists u\in K : uk_1 a_2 = uk_2 a_1
    \]
    then $K^{-1}A$ admits the structure of a pointed act.
\end{construction}
\begin{proof}[Proof]
    The relation is clearly a reflexive and symmetric relation. Assume 
    that $(k_1,a_1)\sim(k_2,a_2)\sim(k_3,a_3)$, so 
    $uk_1a_2 = uk_2a_1$ and $vk_2a_3 = vk_3a_2$ for some $u,v\in K$, then 
    \[
        uvk_2(k_1a_3) = uk_1vk_2a_3 = uk_1vk_3a_2 = vk_3uk_1a_2 = vk_3uk_2a_1 = uvk_2(k_3a_1)
    \] 
    thus the relation is an equivalence. Denote the equivalence classes as $\frac{a}{k} = [a,k]_\sim$. 
    Define the action of $K^{-1}S$ on $K^{-1}A$ by 
    \[
        \frac{s_1}{k_1}\cdot\frac{a}{k} = \frac{s_1 a}{k_1 k}
    \]
    to show well-definedness assume $\frac{s_1}{k_1}=\frac{s_2}{k_2}$ and $\frac{a_1}{l_1}=\frac{a_2}{l_2}$, then 
    we find $u,v\in K$ such that $s_1k_2u = s_2k_1u$ and $vl_1a_2 = vl_2a_1$, then we have 
    \[
        uvk_2l_2s_1a_1 = s_1k_2uvl_2a_1 = s_2k_1uvl_1a_2 = uvk_1l_1s_2a_2 \implies \frac{s_1a_1}{k_1l_1} = \frac{s_2a_2}{k_2l_2}
    \]
    so 
    \[
        \frac{s_1}{k_1}\cdot\frac{a_1}{l_1} = \frac{s_1a_1}{k_1l_1} = \frac{s_2a_2}{k_2l_2} = \frac{s_2}{k_2}\cdot\frac{a_2}{l_2}
    \]
    which proves that the map is well defined. We have to show that it is an action. 
    The associativity and unit axiom is trivial. Define $0 = \frac{0}{1}$, then 
    \[
        0\cdot\frac{a}{k} = \frac{0}{1}\cdot \frac{a}{k} = \frac{0}{k} = \frac{0}{1} = 0
    \]
    since $k\cdot 0 = 1\cdot 0$. Hence it is really a pointed act. 
\end{proof}
\begin{proposition}
    Let $f: A\to B$ be an act homomorphism, then we have an induced 
    map $K^{-1}f : K^{-1}A \to K^{-1}B$ defined as 
    \[
        K^{-1}f\left(\frac{a}{k}\right) = \frac{f(a)}{k}
    \] 
    it is a well-defined homomorphism of acts.
\end{proposition}
\begin{proof}[Proof]
    Assume $\frac{a_1}{k_1}=\frac{a_2}{k_2}$, so we find $u\in K$ such that 
    $uk_2a_1= uk_1a_2$, then 
    \[
        uk_2f(a_1) = f(uk_2a_1) = f(uk_1a_2) = uk_1f(a_2) \implies K^{-1}f\left(\frac{a_1}{k_1}\right)=\frac{f(a_1)}{k_1}=\frac{f(a_2)}{k_2} = K^{-1}f\left(\frac{a_2}{k_2}\right)
    \]
    and 
    \[
    \frac{s_1}{k_1}\cdot K^{-1}f\left(\frac{a}{k}\right) = \frac{s_1f(a)}{k_1k} = \frac{f(s_1a)}{k_1k} = 
    K^{-1}f\left(\frac{s_1a}{k_1k}\right) = K^{-1}f\left(\frac{s_1}{k_1}\cdot\frac{a}{k}\right)
    \]
\end{proof}
\begin{proposition}
    We have a localisation functor $K^{-1}:\actcat{S}\to K^{-1}\actcat{S}$.
\end{proposition}
\begin{proof}[Proof]
    Choose $\frac{a}{k}$, then
    \[
    K^{-1}(g\circ f)\left(\frac{a}{k}\right) = \frac{g(f(a))}{k} = K^{-1}g\left(\frac{f(a)}{k}\right) = (K^{-1}g\circ K^{-1}f)(\frac{a}{k}) \implies K^{-1}(g\circ f) = K^{-1}g\circ K^{-1}f
    \]
    and 
    \[
        K^{-1}1_A\left(\frac{a}{k}\right) = \frac{a}{k} = 1_{K^{-1}A}(\frac{a}{k}) \implies K^{-1}1_A = 1_{K^{-1}A}
    \]
\end{proof}
\subsection{Complexes and exact sequences}
\begin{definition}
    A \textbf{chain complex} $(X_n, d_n: X_{n} \to X_{n-1})_{n\in\mathbb{Z}}$ is a sequence of acts and act morphisms
    \[\begin{tikzcd}
	\dots & {X_{n+1}} & {X_n} & {X_{n-1}} & \dots
	\arrow[from=1-1, to=1-2]
	\arrow["{d_{n+1}}", from=1-2, to=1-3]
	\arrow["{d_n}", from=1-3, to=1-4]
	\arrow[from=1-4, to=1-5]
\end{tikzcd}\]
    such that for all $n\in\mathbb{Z}$ we have $\text{im}(d_{n+1})\subseteq\ker(d_n)$. Chain
    complex is \textbf{Rees exact at $X_n$} if and only if $\text{im}(d_{n+1})=\ker(d_n)$.
    Chain complex is \textbf{Rees exact} if it is Rees exact at $X_n$ for each $n\in\mathbb{Z}$.
\end{definition}
\begin{remark}
    If $(X_n, d_n: X_{n} \to X_{n-1})_{n\in\mathbb{Z}}$ is a sequence of pointed acts, then
    it is a chain complex if and only if $\forall n\in\mathbb{Z} : d_n\circ d_{n+1} = 0$.
\end{remark}
\begin{proof}[Proof]
    First we assume that $d_{n+1}\circ d_n = 0$. Let $(y_1,y_2)\in\text{im}(d_{n+1})$, if 
    $y_1=y_2$ we are done. Suppose then that there are some $x_1,x_2\in X_{n+1}$ such that
    $d_{n+1}(x_1)=y_1$ and $d_{n+1}(x_2) = y_2$, then we have 
    \[
        d_n(y_1) = (d_n\circ d_{n+1})(x_1) = 0 = (d_n\circ d_{n+1})(x_2) = d_n(y_2)
    \]
    therefore $(y_1,y_2)\in\ker(d_m)$, hence $\text{im}(d_{n+1})\subseteq\ker(d_n)$.\par
    Assume now that $\im(d_{n+1})\subseteq\ker(d_n)$. We know that $0\in\im(d_{n+1})$, since
    $d_{n+1}(0) = 0$ therefore for any $x\in X_{n+1}$ by assumption $(0,d_{n+1}(x))\in\ker(d_n)$
    so $d_{n+1}(d_n(x)) = d_n(0) = 0$.
\end{proof}
\begin{definition}
    Let $(X_n, d_n)_{n\in\mathbb{Z}}$ and $(Y_n, e_n)_{n\in\mathbb{Z}}$ be two 
    chain complexes. A \textbf{chain map} between them is a sequence of act morphisms
    $(f_n: X_n\to Y_n)_{n\in\mathbb{Z}}$ such that for all $n\in\mathbb{Z}$ the following square commutes 
    \[\begin{tikzcd}
	{X_{n+1}} && {X_n} \\
	\\
	{Y_{n+1}} && {Y_n}
	\arrow["{d_{n+1}}", from=1-1, to=1-3]
	\arrow["{f_{n+1}}"{description}, from=1-1, to=3-1]
	\arrow["{f_n}"{description}, from=1-3, to=3-3]
	\arrow["{e_{n+1}}", from=3-1, to=3-3]
    \end{tikzcd}\]
\end{definition}
\begin{remark}
    We have that $f_n(\text{Im}(d_{n+1}))\subseteq\text{Im}(e_{n+1})$.
\end{remark}
\begin{definition}
    Let $(X_n, d_n: X_{n} \to X_{n-1})_{n\in\mathbb{Z}}$ be a chain complex,
    then we define \textbf{the $n$-th homology act} by
    \[
        h_n(X) = \ker(d_{n})/\im(d_{n+1}) = \{[x,y] : (x,y)\in\ker(d_n) \}
    \]
    where by $\ker(d_{n})/\im(d_{n+1})$ we mean the Rees factor by a subact.
\end{definition}
Let $(A,a)$ and $(B,b)$ be chain complexes and $f: A \to B$ a chain map, then define 
$h_n(f): h_n(A) \to h_n(B)$ by $[x,y]\mapsto [f_n(x),f_n(y)]$. It is well-defined, since if 
$(x,y)\in\ker(a_n)$, then $b_n(f_n(x)) = f_{n-1}(a_n(x)) = f_{n-1}(a_n(y)) = b_n(f_n(y))$. And if 
we choose $[x_1,y_1]=[x_2,y_2]$, then either $(x_1,y_1)\sim(0,0)\sim (x_2,y_2)$ or $x_1=x_2$ and 
$y_1=y_2$. If the latter then we are done, otherwise it suffices to show
\[
    \left[(x_1,y_1)\sim (0,0) \iff (x_1,y_1)\in\im(a_{n+1})\right] \implies (f_n(x_1),f_n(y_1))\sim (0,0)
\] 
If $x_1=y_1$, then $f_n(x_1)=f_n(y_1)$ which implies $(f(x_1),f(y_1))\sim (0,0)$. 
Otherwise we find $x_2,y_2\in A_{n+1}$ such that $a_{n+1}(x_2)=x_1$ and $a_{n+1}(y_2)=y_1$, but 
then $f_n(x_1) = f_n(a_{n+1}(x_2)) = b_{n+1}(f_{n+1}(x_2))$ and 
$f_n(y_1) = f_n(a_{n+1}(y_2)) = b_{n+1}(f_{n+1}(y_2))$, therefore 
$(f_n(x_1),f_n(y_1))\in\im(b_{n+1}) \iff (f_n(x_1),f_n(y_1))\sim (0,0)$.

\begin{proposition}
    $h_n : Ch(\actcat{S})\to\actcat{S}$ is a functor.
\end{proposition}
\begin{proof}[Proof]
    \[
        h_n(1_A)([x,y]) = [1_{A_n}(x),1_{A_n}(y)] = [x,y] \implies h_n(1_A) = 1_{h_n(A)}
    \]
    \[
        h_n(g\circ f)([x,y]) = [g_n(f_n(x)),g_n(f_n(y))] 
        = h_n(g)([f_n(x), f_n(y)]) = (h_n(g)\circ h_n(f))([x,y])
    \]
    therefore $h_n(g\circ f) = h_n(g)\circ h_n(f)$.
\end{proof}
From now on by acts we will mean pointed acts, objects of the category $\actcat{S}_0$. 
\begin{definition}
    \textbf{Rees short exact sequence} is a sequence of acts
    \[\begin{tikzcd}
        0 & A & B & C & 0
        \arrow[from=1-1, to=1-2]
        \arrow["f", from=1-2, to=1-3]
        \arrow["g", from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    exact at $A,B$ and $C$.
\end{definition}
\begin{remark}
    By definition of exactness it follows that $\ker(f) = \{(0,0)\}\cup\Delta_L$ and 
    $\im(g) = N\times N$, therefore $f$ is injective and $g$ is surjective. Similarly
    injectivity of $f$ and surjectivity of $g$ imply exactness 
    at $A$ and $C$ respectively.
\end{remark}
\begin{example}
    Let $A$ and $B$ be acts, then we have the following sequence
    \[\begin{tikzcd}
        0 & A & {A\oplus B} & B & 0
        \arrow[from=1-1, to=1-2]
        \arrow["\iota", from=1-2, to=1-3]
        \arrow["\lambda", from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    where $\iota: x \mapsto (x,0)$ and 
    \[
        \lambda(x,y) = 
        \begin{cases}
            y, & \text{if } x=0 \\
            0, & \text{otherwise}
        \end{cases}
    \]
    the exactness at $A$ and $B$ is easily seen. We will show exactness 
    at $A\oplus B$.\par
    Choose $((x_1,y_1),(x_2,y_2))\in\im(\iota)$, if $x_1=x_2$ and $y_1=y_2$ we are done, 
    otherwise we have $y_1=0=y_2$, therefore 
    \[
        \lambda(x_1,y_1)=0=\lambda(x_2,y_2)
    \]
    which implies $\im(\iota)\subseteq\ker(\lambda)$.
    Let $((x_1,y_1),(x_2,y_2))\in\ker(\lambda)$, if $x_1=x_2$ and $y_1=y_2$
    we are done, therefore assume that $x_1\neq x_2$ or $y_1\neq y_2$.\par
    Suppose $x_1=0$, then $x_2\neq 0$ (else $y_1=y_2$), this implies
    that $y_2=0$ and $y_1=\lambda(x_1,y_1)=\lambda(x_2,y_2)=0$
    so $(x_1,y_1)=(0,0)$ and $y_2=0$, then 
    $\iota(x_2)=(x_2,y_2)$ and $\iota(0)=(x_1,y_1)$.\par
    Suppose now that $x_1\neq 0$, then $y_1=0$ and 
    $\lambda(x_2,y_2)=\lambda(x_1,y_1)=0$, which means either 
    $(x_2,y_2)=(0,0)$ or that $x_2\neq 0$, in either case $y_2=0$,
    hence $\iota(x_1)=(x_1,y_1)$ and $\iota(x_2)=(x_2,y_2)$.
    In conclusion $\ker(\lambda)=\im(\iota)$ as required.
\end{example}
\begin{example}
    If $B$ is a subact of $A$, then the following sequence is Rees short exact 
    \[\begin{tikzcd}
        0 & B & A & {A/B} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["\iota", from=1-2, to=1-3]
        \arrow["\pi", from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    where $\iota$ is the set inclusion map and $\pi$ is the canonical projection. 
    The exactness at $A$ and $A/B$ is trivial. Choose $x\in B$, then we have 
    $\pi(\iota(x)) = \pi(x) = [x] = [0] \implies \im(\iota)\subseteq\ker(\pi)$. Choose 
    $(x,y)\in\ker(\pi)$, so $[x]=[y]$, if $x=y$, then its trivially in $\im(\iota)$, else 
    we necessarily have $[x]=[0]=[y]$, so $x,y\in B$ as required.
\end{example}
\begin{proposition}
    Assume that $\mathcal{S}$ is a commutative, pointed monoid and $K\subseteq S$ is a submonoid of $S$ such that $0\not\in K$, then
    given a Rees short exact sequence 
    \[\begin{tikzcd}
        0 & A & B & C & 0
        \arrow[from=1-1, to=1-2]
        \arrow[from=1-2, to=1-3]
        \arrow[from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    the following sequence is Rees short exact
    \[\begin{tikzcd}
        0 & {K^{-1}A} & {K^{-1}B} & {K^{-1}C} & 0
        \arrow[from=1-1, to=1-2]
        \arrow[from=1-2, to=1-3]
        \arrow[from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
\end{proposition}
\begin{proof}[Proof]
    Let $\frac{a}{k}\in K^{-1}A$, then 
    \[
    (K^{-1}g\circ fK^{-1}f)\left(\frac{a}{k}\right) = \frac{g(f(a))}{k} = \frac{0}{k} =\frac{0}{1}=0 \implies \im(K^{-1}f)\subseteq\ker(K^{-1}g)
    \]
    Assume that $\frac{g(b_1)}{k_1}=\frac{g(b_2)}{k_2}$, so we find $u\in K$ such that 
    $g(uk_1b_2)=uk_1g(b_2)=uk_2g(b_1)=g(uk_2b_1)$, therefore 
    $(uk_1b_2,uk_2b_1)\in\im(f)$. If $uk_1b_2=uk_2b_1$ we are done, otherwise 
    we find $z_1,z_2\in A$ such that $f(z_1) = uk_1b_2$ and $f(z_2)=uk_2b_1$.
    then 
    \[
        K^{-1}f\left(\frac{z_1}{uk_1k_2}\right) = \frac{uk_1b_2}{uk_1k_2} = \frac{b_2}{k_2}
    \] 
    \[
        K^{-1}f\left(\frac{z_2}{uk_2k_1}\right) = \frac{uk_2b_1}{uk_2k_1} = \frac{b_1}{k_1}
    \]
    which proves exactness at $K^{-1}B$, we have to show that $K^{-1}$ preserves injections and surjections.
    Suppose $\frac{f(a_1)}{k_1}=\frac{f(a_2)}{k_2}$, then find $u\in K^{-1}$ such that 
    \[
    uk_1f(a_2) = uk_2f(a_1)\implies f(uk_1a_2)=f(uk_2a_1) \implies uk_1a_2=uk_2a_1 \implies\frac{a_1}{k_1}=\frac{a_2}{k_2}
    \]
    To prove surjectivity choose $\frac{z}{k}\in K^{-1}C$, we find $y\in B$ such that $g(y)=z$, then 
    \[
        K^{-1}g\left(\frac{y}{k}\right) = \frac{g(y)}{k} = \frac{z}{k}
    \]
    which completes the proof.
\end{proof}
\begin{corollary}
    Let $B$ be a subact of $A$, then $K^{-1}B$ is a subact of $K^{-1}A$ and  
    \[
        K^{-1}\left(\frac{A}{B}\right) \simeq \frac{K^{-1}A}{K^{-1}B}
    \]
\end{corollary}
\begin{proof}[Proof]
    Consider the Rees short exact sequence 
    \[\begin{tikzcd}
        0 & {K^{-1}B} & {K^{-1}A} & {K^{-1}(A/B)} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["{K^{-1}\iota}", from=1-2, to=1-3]
        \arrow["{K^{-1}\pi}", from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    $K^{-1}B$ is clearly a subset of $K^{-1}A$, it contains zero, since $B$ contains zero and 
    it is closed under the monoidal action, because $B$ is.
    First we need to show that $\im K^{-1}\iota = \rho_{K^{-1}B}$, we have 
    \[
    \left(\frac{b_1}{k_1},\frac{b_2}{k_2}\right)\in\im(S^{-1}\iota) \iff \frac{b_1}{k_1}=\frac{b_2}{k_2}\lor\exists\,\frac{a_1}{l_1},\frac{a_2}{l_1}\in K^{-1}B :
    \frac{a_1}{l_1}=\frac{b_1}{k_1}\land\frac{a_2}{l_2}=\frac{b_2}{k_2}
    \]
    \[
        \iff \frac{b_1}{k_1}=\frac{b_2}{k_2}\lor \frac{b_1}{k_1},\frac{b_2}{k_2}\in K^{-1}B \iff \left(\frac{a_1}{k_1},\frac{a_2}{k_2}\right)\in\rho_{K^{-1}B}
    \]
    then by the first isomorphism theorem we have 
    \[
    K^{-1}(A/B)\simeq K^{-1}(A)/\ker(K^{-1}\pi) = K^{-1}(A)/\im(K^{-1}\iota) = K^{-1}A/K^{-1}B
    \]
\end{proof}
\begin{definition}
    Let $A$ be an $\mathcal{S}$ act. We say, that $A$ is \textbf{torsion free}, if 
    for each $r\in S$ a left cancellative element we have 
    \[
        \forall x,y\in S : r(x)=r(y) \implies x=y
    \]
\end{definition}
\begin{definition}
    We call an $\mathcal{S}$ act $A$ \textbf{finitely generated}, if there exist 
    $x_1,\dots,x_n\in A$ such that $A = \bigcup_{i=1}^n S\cdot x_i$. Then we say 
    that the set $\{x_1,\dots,x_n\}$ is a \textbf{generating set} of $A$.
\end{definition}
\begin{proposition}
    Consider the Rees short exact sequence of acts 
    \[\begin{tikzcd}
        0 & A & B & C & 0
        \arrow[from=1-1, to=1-2]
        \arrow["f", from=1-2, to=1-3]
        \arrow["g", from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    if $A$ and $C$ are torsion free, then $B$ is torsion free.
\end{proposition}
\begin{proof}[Proof]
    Suppose $rx=ry$ and assume for a contradiction that $x\neq y$. Then we have 
    $rg(x)=rg(y) \implies g(x)=g(y)$, by exactness there exists $z,w\in A$ such that 
    $f(z)=x$ and $f(w)=y$, then $f(rz)=rf(z)=rx=ry=rf(w)=f(rw) \implies rz=rw$, then 
    $z=w$, since $A$ is torsion free, which implies $x=y$ a contradiction.
\end{proof}
\begin{corollary}
    Coproduct of torsion free acts is torsion free.
\end{corollary}
\begin{proposition}
    Consider the following Rees short exact sequence 
    \[\begin{tikzcd}
	0 & A & B & C & 0
	\arrow[from=1-1, to=1-2]
	\arrow["f", from=1-2, to=1-3]
	\arrow["g", from=1-3, to=1-4]
	\arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    if $A$ and $C$ are finitely generated, then so is $B$.
\end{proposition}
\begin{proof}[Proof]
    Let $\{x_1,\dots,x_n\}$ and $\{z_1,\dots,z_m\}$ be the generating sets 
    of $A$ and $C$ respectively. From surjectivity of $g$, we find $y_i$ for each $i\in\{1,\dots,m\}$
    such that $g(y_i)=z_i$. We will show that the set \[\{f(x_1),\dots,f(x_n),y_1,\dots,y_m\}\] 
    generates $B$.\par Choose $y\in B$, then we find $r\in S$ and $z_i\in C$ such that 
    $g(y)=r\cdot z_i = r\cdot g(y_i) = g(r\cdot y_i)$, so by exactness we have 
    $(y,r\cdot y_i)\in\im(f)$. If $y=r\cdot y_i$ we are done, otherwise we find 
    $x\in A$ such that $f(x)=y$. Since $A$ is finite generated there is $s\in S$ and 
    $x_i\in A$ such that $r\cdot x_i = x$, then $y=f(x)=r\cdot f(x_i)$, which completes the proof. 
\end{proof}
\begin{lemma}
    Consider the following commutative diagram
    \[\begin{tikzcd}
	0 & A & B & C & 0 \\
	0 & {A'} & {B'} & {C'} & 0
	\arrow[from=1-1, to=1-2]
	\arrow["f", from=1-2, to=1-3]
	\arrow["\alpha", from=1-2, to=2-2]
	\arrow["g", from=1-3, to=1-4]
	\arrow["\beta", from=1-3, to=2-3]
	\arrow[from=1-4, to=1-5]
	\arrow["\gamma", from=1-4, to=2-4]
	\arrow[from=2-1, to=2-2]
	\arrow["{f'}", from=2-2, to=2-3]
	\arrow["{g'}", from=2-3, to=2-4]
	\arrow[from=2-4, to=2-5]
\end{tikzcd}\]
    where $\alpha, \beta, \gamma$ are isomorphisms, 
    then the first row is Rees exact iff the second row is.
\end{lemma}
\begin{proof}[Proof]
    \par Assume that the first row is Rees exact. We will prove that 
    $f'$ is injective. Let $f'(x)=f'(y)$, then there are unique 
    $x_1,y_1\in A$ such that $\alpha(x_1) = x$ and $\alpha(y_1) = y$, 
    by commutativity of the left square $\beta(f(x_1))=f'(\alpha(x_1))=f'(\alpha(y_1))=\beta(f(y_1))$ so 
    $f(x_1)=f(y_1)$ and since $f$ is injective $x_1=y_1$. Choose any $y\in C'$,
    then we find unique $y_1\in C$ such that $\gamma(y_1) = y$, since $g$ is surjective
    there is some $y_2\in B$ such that $g(y_2) = y_1$. By the commutativity of the right square
    we have $g'(\beta(y_2)) = \gamma(g(y_2)) = y$, therefore $g'$ is surjective.
    \par It remains to prove exactness at $B'$, let $(x,y)\in\ker(g')$ so $g'(x)=g'(y)$. 
    If $x=y$ we are done, assume then that $x\neq y$. We find unique $x_1,y_1\in B$ such that 
    $\beta(x_1) = x$ and $\beta(y_1) = y$. By commutativity of the right square we get
    $\gamma(g(x_1)) = \gamma(g(y_1)) \implies g(x_1)=g(y_1)$. By exactness at $B$ 
    it follows that $(x_1,y_1)\in\im(f)$, since $x\neq y$ there are some 
    $x_2,y_2\in A$ such that $f(x_2)=x_1$ and $f(y_2)=y_1$, thus 
    by commutativity of the left square $f'(\alpha(x_2)) = \beta(f(x_2)) = x$ 
    and $f'(\alpha(y_2)) = \beta(f(y_2)) = y$ or $(x,y)\in\im(f')$.
    \par Choose any $(x,y)\in\im(f')$, if $x=y$ we are done, so assume $x\neq y$,
    then there are some $x_1,y_1\in A'$ such that $f'(x_1)=x$ and $f'(y_1)=y$. 
    Since $\alpha$ is an iso there are unique $x_2,y_2\in A$ so that 
    $\alpha(x_2)=x_1$ and $\alpha(y_2)=y_1$, then by commutativity of the 
    left square $x = \beta(f(x_2))$ and $y=\beta(f(y_2))$, by exactness at $B$
    $g(f(x_2))=g(f(y_2))$, since $(f(x_2),f(y_2))\in\im(f)$ by definition.
    Then we have $\gamma(g(f(x_2))) = \gamma(g(f(y_2)))$ and therefore 
    by commutativity of the outer rectangle 
    $g'(x) = g'(f'(\alpha(x_2))) = \gamma(g(f(x_2))) = \gamma(g(f(y_2))) = g'(f'(\alpha(y_2))) = g'(y)$
    which means $(x,y)\in\ker(g')$. It follows that $\ker(g') = \im(f')$ as 
    required.
    \par Assume now that the second row is Rees exact. We can consider the following 
    commutative diagram 
    \[\begin{tikzcd}
        0 & {A'} & {B'} & {C'} & 0 \\
        0 & A & B & C & 0
        \arrow[from=1-1, to=1-2]
        \arrow["{f'}", from=1-2, to=1-3]
        \arrow["{\alpha^{-1}}", from=1-2, to=2-2]
        \arrow["{g'}", from=1-3, to=1-4]
        \arrow["{\beta^{-1}}", from=1-3, to=2-3]
        \arrow[from=1-4, to=1-5]
        \arrow["{\gamma^{-1}}", from=1-4, to=2-4]
        \arrow[from=2-1, to=2-2]
        \arrow["f", from=2-2, to=2-3]
        \arrow["g", from=2-3, to=2-4]
    	\arrow[from=2-4, to=2-5]
    \end{tikzcd}\]
    and notice that the result follows by symmetry from the first part of the proof. 
\end{proof}

In contrast with the category of modules, the 
internal hom functor isn't left exact. Indeed, consider the following
counterexample.
\begin{counterexample}
    Recall that $\mathbb{Z}_2$-acts are just pointed sets. Consider a 
    short exact sequence of pointed sets
    \[\begin{tikzcd}
        0 & {\mathbb{Z}_2} & {\mathbb{Z}_3} & {\mathbb{Z}_3/\mathbb{Z}_2} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["\iota", hook, from=1-2, to=1-3]
        \arrow["\pi", two heads, from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    where $\iota$ is the inclusion map and $\pi$ is the canonical projection.
    We will show that the following sequence isn't exact at $\text{Hom}(\mathbb{Z}_3,\mathbb{Z}_3)$
    \[\begin{tikzcd}
        0 & {\text{Hom}(\mathbb{Z}_3,\mathbb{Z}_2)} & {\text{Hom}(\mathbb{Z}_3,\mathbb{Z}_3)} & {\text{Hom}(\mathbb{Z}_3,\mathbb{Z}_3/\mathbb{Z}_2)}
        \arrow[from=1-1, to=1-2]
        \arrow["{\iota_*}", from=1-2, to=1-3]
        \arrow["{\pi_*}", from=1-3, to=1-4]
    \end{tikzcd}\]
    the elements of acts in the sequence are as below
    \begin{table}[H]
        \centering
        \begin{tabular}{ccccccccc}
        \hline
        \multicolumn{9}{|c|}{$\text{Hom}(\mathbb{Z}_3,\mathbb{Z}_3)$}                                                                                                                                                                                                                                                                              \\ \hline
        \multicolumn{1}{|c|}{$0$}          & \multicolumn{1}{c|}{$f_1$}        & \multicolumn{1}{c|}{$f_2$}        & \multicolumn{1}{c|}{$f_3$}        & \multicolumn{1}{c|}{$f_4$}        & \multicolumn{1}{c|}{$f_5$}          & \multicolumn{1}{c|}{$f_6$}          & \multicolumn{1}{c|}{$f_7$}          & \multicolumn{1}{l|}{$f_8$}          \\ \hline
        \multicolumn{1}{|c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{$0\mapsto 0$}   & \multicolumn{1}{c|}{$0\mapsto 0$}   & \multicolumn{1}{c|}{$0\mapsto 0$}   & \multicolumn{1}{c|}{$0\mapsto 0$}   \\ \hline
        \multicolumn{1}{|c|}{$1\mapsto 0$} & \multicolumn{1}{c|}{$1\mapsto 0$} & \multicolumn{1}{c|}{$1\mapsto 0$} & \multicolumn{1}{c|}{$1\mapsto 1$} & \multicolumn{1}{c|}{$1\mapsto 1$} & \multicolumn{1}{c|}{$1\mapsto 1$}   & \multicolumn{1}{c|}{$1\mapsto 2$}   & \multicolumn{1}{c|}{$1\mapsto 2$}   & \multicolumn{1}{c|}{$1\mapsto 2$}   \\ \hline
        \multicolumn{1}{|c|}{$2\mapsto 0$} & \multicolumn{1}{c|}{$2\mapsto 1$} & \multicolumn{1}{c|}{$2\mapsto 2$} & \multicolumn{1}{c|}{$2\mapsto 0$} & \multicolumn{1}{c|}{$2\mapsto 1$} & \multicolumn{1}{c|}{$2\mapsto 2$}   & \multicolumn{1}{c|}{$2\mapsto 0$}   & \multicolumn{1}{c|}{$2\mapsto 1$}   & \multicolumn{1}{c|}{$2\mapsto 2$}   \\ \hline
                                           &                                   &                                   &                                   &                                   &                                     &                                     &                                     &                                     \\ \cline{1-4} \cline{6-9} 
        \multicolumn{4}{|c|}{$\text{Hom}(\mathbb{Z}_3,\mathbb{Z}_2)$}                                                                                  & \multicolumn{1}{c|}{}             & \multicolumn{4}{c|}{$\text{Hom}(\mathbb{Z}_3,\mathbb{Z}_3/\mathbb{Z}_2)$}                                                                             \\ \cline{1-4} \cline{6-9} 
        \multicolumn{1}{|c|}{$0$}          & \multicolumn{1}{c|}{$f_1$}        & \multicolumn{1}{c|}{$f_2$}        & \multicolumn{1}{c|}{$f_3$}        & \multicolumn{1}{c|}{}             & \multicolumn{1}{c|}{$0$}            & \multicolumn{1}{c|}{$f_1$}          & \multicolumn{1}{c|}{$f_2$}          & \multicolumn{1}{c|}{$f_3$}          \\ \cline{1-4} \cline{6-9} 
        \multicolumn{1}{|c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{$0\mapsto 0$} & \multicolumn{1}{c|}{}             & \multicolumn{1}{c|}{$0\mapsto [0]$} & \multicolumn{1}{c|}{$0\mapsto [0]$} & \multicolumn{1}{c|}{$0\mapsto [0]$} & \multicolumn{1}{c|}{$0\mapsto [0]$} \\ \cline{1-4} \cline{6-9} 
        \multicolumn{1}{|c|}{$1\mapsto 0$} & \multicolumn{1}{c|}{$1\mapsto 0$} & \multicolumn{1}{c|}{$1\mapsto 1$} & \multicolumn{1}{c|}{$1\mapsto 1$} & \multicolumn{1}{c|}{}             & \multicolumn{1}{c|}{$1\mapsto [0]$} & \multicolumn{1}{c|}{$1\mapsto [0]$} & \multicolumn{1}{c|}{$1\mapsto [2]$} & \multicolumn{1}{c|}{$1\mapsto [2]$} \\ \cline{1-4} \cline{6-9} 
        \multicolumn{1}{|c|}{$2\mapsto 0$} & \multicolumn{1}{c|}{$2\mapsto 1$} & \multicolumn{1}{c|}{$2\mapsto 0$} & \multicolumn{1}{c|}{$2\mapsto 1$} & \multicolumn{1}{c|}{}             & \multicolumn{1}{c|}{$2\mapsto [0]$} & \multicolumn{1}{c|}{$2\mapsto [2]$} & \multicolumn{1}{c|}{$2\mapsto [0]$} & \multicolumn{1}{c|}{$2\mapsto [2]$} \\ \cline{1-4} \cline{6-9} 
        \end{tabular}
        \end{table}
    we now see that 
    $\iota\circ f_1 = f_1$, $\iota\circ f_2 = f_3$, $\iota\circ f_3 = f_4$, therefore we have
    \[
        \im(\iota_*) = \{(f_1,f_3),(f_3,f_1),(f_1,f_4),(f_4,f_1),(f_3,f_4),(f_4,f_3)\}\cup\{(0,0),\cdots,(f_8,f_8)\}
    \]
    but $\pi\circ f_2 = f_1 = \pi\circ f_5$ hence $(f_2,f_5)\in\ker(\pi_*)$, however $(f_2,f_5)\not\in\im(\iota_*)$.
\end{counterexample}
    Weaker result holds, the covariant internal hom functor does preserve chain complexes.
\begin{proposition}
    Let $(X_n, d_n)_{n\in\mathbb{Z}}$ be a chain complex and $D$ an act, then 
    $(\text{Hom}(D,X_n),{d_n}_*)$ is a chain complex.
\end{proposition}
\begin{proof}[Proof]
    Choose $n\in\mathbb{Z}$ and $(f_1,f_2)\in\im({d_{n+1}}_*)$, if 
    $f_1=f_2$ we are done, otherwise we find $h_1,h_2\in\text{Hom}(D,X_{n+1})$
    such that $d_{n+1}\circ h_1 = f_1$ and $d_n\circ h_2 = f_2$, then 
    for all $x\in D$ we have $(f_1(x),f_2(x))\in\im(d_{n+1})\subseteq\ker(d_n)$,
    which implies $d_n\circ f_1(x)=d_n\circ f_2(x)$ so $(f_1,f_2)\in\ker({f_n}_*)$.
\end{proof}
\begin{lemma}[Four lemma]
    Consider the following commutative diagram, whose rows are exact
    \[\begin{tikzcd}
        A & B & C & D \\
        {A'} & {B'} & {C'} & {D'}
        \arrow["f", from=1-1, to=1-2]
        \arrow["r", from=1-1, to=2-1]
        \arrow["g", from=1-2, to=1-3]
        \arrow["s", from=1-2, to=2-2]
        \arrow["h", from=1-3, to=1-4]
        \arrow["t", from=1-3, to=2-3]
        \arrow["u", from=1-4, to=2-4]
        \arrow["{f'}", from=2-1, to=2-2]
        \arrow["{g'}", from=2-2, to=2-3]
        \arrow["{h'}", from=2-3, to=2-4]
    \end{tikzcd}\]
    then 
    \begin{enumerate}
        \item If $s,u$ are monomorphisms and $r$ is an epimorphism, then $t$ is a monomorphism.
        \item If $r,t$ are epimorphisms and $u$ is a monomorphism, then $s$ is an epimorphsm.
    \end{enumerate}
\end{lemma}
\begin{proof}[Proof]
    To prove the first part choose $(x,y)\in\ker(t)$ and assume for a contradiction
    that $x\neq y$. By commutativity of the right square we have 
    $u(h(x))=u(h(y)) \implies h(x)=h(y)$ since $u$ is a mono by assumption.
    By exactness at $C$ and the fact that $x\neq y$ there are $x_1,y_1\in B$ such that 
    $g(x_1)=x$ and $g(y_1)=y$ and $x_1\neq y_1$, then $s(x_1)\neq s(y_1)$, since 
    $s$ is injective by assumption, moreover we have  
    \[t(g(x_1))=t(x)=t(y)=t(g(y_1))\]
    hence $g'(s(x_1))=g'(s(y_1))$, which implies 
    $(s(x_1)),s(y_1))\in\im f'$ by exactness at $B'$, since 
    $s(x_1)\neq s(y_1)$ there are $x_2,y_2\in A'$ such that 
    $f'(x_2)=s(x_1)$ and $f'(y_2)=s(y_1)$. By surjecitvity of $r$
    we find $x_3,y_3\in A$ such that 
    $r(x_3)=x_2$ and $r(y_3)=y_2$, then 
    \[
        s(f(x_3))=f'(r(x_3)) = f'(x_2) = s(x_1)
    \]
    \[
        s(f(y_3))=f'(r(y_3)) = f'(y_2) = s(y_1)
    \]
    by injectivity of $s$ we have 
    $f(x_3)=x_1$ and $f(y_3)=y_1$, therefore by exactness at $B$
    \[
        x = g(x_1) = g(y_1) = y
    \]
    we have reached a contradiction.\par
    To prove the second part choose $y\in C'$, if $y=0$ we are done, since $s(0)=0$, assume
    that $y\neq 0$, then since $t$ is surjective we find $y_1\in C$ such that 
    $t(y_1)=g'(y)$, then by exactness at $C'$ and commutativity of the
    right square we have $u(0) = 0 = h'(g'(y)) = h'(t(y_1)) = u(h(y_1))$,
    hence $h(0)=0=h(y_1)$, since $u$ is injective. We now have that 
    $(0,y_1)\in\im(g)$ by exactness at $C$.\par 
    If $y_1\neq 0$ we find $y_2\in B$ such that 
    $g(y_2) = y_1$, then 
    \[
        g'(s(y_2)) = t(g(y_2)) = t(y_1) = g'(y)
    \] 
    so by exactness at $B'$ we have $(s(y_2),y)\in\im(f')$, if $s(y_2)=y$ we are done
    otherwise we find $z\in A'$ such that $f'(z)=y$ and $z_1\in A$
    such that $r(z_1)=z$, then $y=f'(r(z_1)) = s(f(z_1))$ by the commutativity
    of the left square.\par
    If $y_1=0$, then $g'(y)=t(y_1)=0=g'(0)$, therefore $(y,0)\in\im(f')$
    by exactness at $B'$, by assumption $y\neq 0$ so there exists $z\in A'$ 
    such that $f'(z)=y$ and $z_1\in A$ satisfying $r(z_1)=z$ and again we have
    by commutativity of the left square   
    \[
        y = f'(r(z_1)) = s(f(z_1))
    \]
    which concludes the proof.
\end{proof}
\begin{corollary}[Five lemma]
    Consider the following commutative diagram with exact rows
    \[\begin{tikzcd}
        A & B & C & D & E \\
        {A'} & {B'} & {C'} & {D'} & {E'}
        \arrow["f", from=1-1, to=1-2]
        \arrow["r", from=1-1, to=2-1]
        \arrow["g", from=1-2, to=1-3]
        \arrow["s", from=1-2, to=2-2]
        \arrow["h", from=1-3, to=1-4]
        \arrow["t", from=1-3, to=2-3]
        \arrow["k", from=1-4, to=1-5]
        \arrow["u", from=1-4, to=2-4]
        \arrow["v", from=1-5, to=2-5]
        \arrow["{f'}", from=2-1, to=2-2]
        \arrow["{g'}", from=2-2, to=2-3]
        \arrow["{h'}", from=2-3, to=2-4]
        \arrow["{k'}", from=2-4, to=2-5]
    \end{tikzcd}\]
    if $s,u$ are isomorphisms, $r$ is an epimorphism and $v$ is a monomorphism, then $t$ is an isomorphism.
\end{corollary}
\begin{proof}[Proof]
    By applying the first and second part of the lemma to our diagram we 
    get that $t$ is a monomorphism and an epimorphism, therefore an isomorphism, 
    since the category of acts is balanced.
\end{proof}
\begin{corollary}[Short five lemma]
    Consider the following commutative diagram with Rees exact rows
    \[\begin{tikzcd}
        0 & A & B & C & 0 \\
        0 & {A'} & {B'} & {C'} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["f", from=1-2, to=1-3]
        \arrow["r", from=1-2, to=2-2]
        \arrow["g", from=1-3, to=1-4]
        \arrow["s", from=1-3, to=2-3]
        \arrow[from=1-4, to=1-5]
        \arrow["t", from=1-4, to=2-4]
        \arrow[from=2-1, to=2-2]
        \arrow["{f'}", from=2-2, to=2-3]
        \arrow["{g'}", from=2-3, to=2-4]
        \arrow[from=2-4, to=2-5]
    \end{tikzcd}\]
    \begin{enumerate}
        \item If $r$ and $t$ are monomorphisms, then $s$ is a monomorphism.
        \item If $r$ and $t$ are epimorphisms, then $s$ is an epimorphism.
        \item If $r$ and $t$ are isomorphism, then $s$ is an isomorphim. 
    \end{enumerate}
\end{corollary}
\begin{definition}
    A sequence of chain complexes 
\[\begin{tikzcd}
	0 & {(A,a)} & {(B,b)} & {(C,c)} & 0
	\arrow[from=1-1, to=1-2]
	\arrow["f", from=1-2, to=1-3]
	\arrow["g", from=1-3, to=1-4]
	\arrow[from=1-4, to=1-5]
\end{tikzcd}\]
    (where by $0$ we mean $\cdots\to 0\to0\to0\to\cdots$) is exact 
    if for each $n\in\mathbb{Z}$ we have that 
    \[\begin{tikzcd}
	0 & {A_n} & {B_n} & {C_n} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["{f_n}", from=1-2, to=1-3]
        \arrow["{g_n}", from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    is a Rees short exact sequence.
\end{definition}
\begin{proposition}
    If 
        \[\begin{tikzcd}
        0 & {(A,a)} & {(B,b)} & {(C,c)} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["f", from=1-2, to=1-3]
        \arrow["g", from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    is a short exact sequence of chain complexes, then the sequence
    \[\begin{tikzcd}
	{h_n(A)} & {h_n(B)} & {h_n(C)}
	\arrow["{h_n(f)}", from=1-1, to=1-2]
	\arrow["{h_n(g)}", from=1-2, to=1-3]
    \end{tikzcd}\]
    is Rees exact.
\end{proposition}
\begin{proof}[Proof]
    Choose $[x,y]\in h_n(A)$, then we have 
    \[
        (h_n(g)\circ h_n(f))([x,y]) = [g_n(f_n(x)),g_n(f_n(y))] = [0,0] \implies \im(h_n(f))\subseteq\ker(h_n(g))
    \]
\end{proof}
\begin{proposition}
    Consider a finite nonempty collection of Rees short exact sequences 
    \[\begin{tikzcd}
        0 & {X_1} & {Y_1} & {Z_1} & 0 \\
        0 & {X_n} & {Y_n} & {Z_n} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["{f_1}", from=1-2, to=1-3]
        \arrow["\vdots"{description, pos=0.4}, draw=none, from=1-2, to=2-2]
        \arrow["{g_1}", from=1-3, to=1-4]
        \arrow["\vdots"{description, pos=0.4}, draw=none, from=1-3, to=2-3]
        \arrow[from=1-4, to=1-5]
        \arrow["\vdots"{description, pos=0.4}, draw=none, from=1-4, to=2-4]
        \arrow[from=2-1, to=2-2]
        \arrow["{f_n}", from=2-2, to=2-3]
        \arrow["{g_n}", from=2-3, to=2-4]
        \arrow[from=2-4, to=2-5]
    \end{tikzcd}\]
    then the following sequence is Rees exact
    \[\begin{tikzcd}
        0 & {\displaystyle\bigoplus_{i=1}^n X_i} & {\displaystyle\bigoplus_{i=1}^n Y_i} & {\displaystyle\bigoplus_{i=1}^n Z_i} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["f", from=1-2, to=1-3]
        \arrow["g", from=1-3, to=1-4]
        \arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
\end{proposition}
\begin{proof}[Proof]
    Choose $(a_1,\dots, a_n)\in\bigoplus_{i=1}^n X_i$, then we find $k\in\{1,\dots,n\}$ 
    such that $a_1=\dots=a_{k-1}=a_{k+1}=\dots=a_n=0$, hence 
    \[
        (g\circ f)(a_1,\dots, a_n) = (0,\dots,g_k(f_k(a_k)),\dots,0) = (0,\dots,0)\implies\im(f)\subseteq\ker(g)
    \]
    Choose $((a_1,\dots,a_n),(b_1,\dots,b_n))\in\ker(g)$ and assume that $(a_1,\dots,a_n)\neq(b_1,\dots,b_n)$, 
    otherwise we are done. We find $i,j\in\{1,\dots,n\}$ such that 
    $a_1=\dots=a_{i-1}=a_{i+1}=\dots=a_n=0$ and $b_1=\dots=b_{j-1}=b_{j+1}=\dots=b_n=0$. \par 
    If $i=j$, then $g_i(a_i)=g_i(b_i)$, therefore $(a_i,b_i)\in\im(f_i)$, by assumption we have $a_i\neq b_i$, so 
    we find $z,w\in X_i$ such that $f_i(z)=a_i$ and $f_i(w)=b_i$, then 
    \[
        f(0,\dots,0,z,0,\dots,0) = (0,\dots,0,a_i,0,\dots,0) = (a_1,\dots, a_n)
    \]
    \[
        f(0,\dots,0,w,0,\dots,0) = (0,\dots,0,b_i,0,\dots,0) = (b_1,\dots, b_n)
    \]
    If $i\neq j$, then $g_i(a_i) = 0$ and $g_j(b_j) = 0$. If $a_i=0$ then $(0,\dots,0)\mapsto(a_1,\dots,a_n)$, if 
    $a_i\neq 0$ we find $z\in X_i$ such that $f_i(z)=a_i$, and again 
    \[
        f(0,\dots,0,z,0,\dots,0) = (0,\dots,0,a_i,0,\dots,0) = (a_1,\dots, a_n)
    \]
    similarly for $b_i$, therefore $\ker(g)\subseteq\im(f)$, which proves exactness at $\bigoplus_{i=1}^n Y_i$.\par 
    Let $(a_1,\dots,a_n)\in\bigoplus_{i=1}^n Z_i$, then find $i$ such that $a_j=0$ for $j\neq i$, since 
    $g_i$ is surjective we find $z\in Y_i$ such that $g_i(z)=a_i$, then $g(0,\dots,0,z,0,\dots,0) = (a_1,\dots,a_n)$.
    Thus $g$ is surjective.\par 
    Suppose $f(a_1,\dots,a_n)=f(b_1,\dots,b_n)$, then we again find $i,j\in\{1,\dots,n\}$, such that 
    $a_k=0$ for $k\neq i$ and $b_=0$ for $l\neq j$. \par If $i\neq j$, then we have $f(a_i)=0=f(0) \implies a_i=0$ and 
    $f(b_j)=0=f(0) \implies b_j=0$, then we have trivially $(a_1,\dots,a_n)=(b_1,\dots,b_n)$. 
    If $i=j$, then $f_i(a_i)=f_i(b_i) \implies a_i=b_i$, so $(a_1,\dots,a_n)=(b_1,\dots,b_n)$. 
\end{proof}
\begin{proposition}
    Consider a short exact sequence of chain complexes
    \[\begin{tikzcd}
	0 & {(A,c)} & {(B,d)} & {(C,e)} & 0
	\arrow[from=1-1, to=1-2]
	\arrow["f", from=1-2, to=1-3]
	\arrow["g", from=1-3, to=1-4]
	\arrow[from=1-4, to=1-5]
    \end{tikzcd}\]
    if any pair of chain complexes $A$,$B$ and $C$ is exact, so is the third.
\end{proposition}
\begin{proof}[Proof]
    Choose $n\in\mathbb{Z}$ and assume $B$ and $C$ are exact. We 
    have the following commutative diagram
    \[\begin{tikzcd}
        0 & {A_{n+1}} & {B_{n+1}} & {C_{n+1}} & 0 \\
        0 & {A_n} & {B_n} & {C_n} & 0 \\
        0 & {A_{n-1}} & {B_{n-1}} & {C_{n-1}} & 0 \\
        0 & {A_{n-2}} & {B_{n-2}} & {C_{n-2}} & 0
        \arrow[from=1-1, to=1-2]
        \arrow["{{{f_{n+1}}}}", hook', from=1-2, to=1-3]
        \arrow["{{{a_{n+1}}}}", from=1-2, to=2-2]
        \arrow["{{{g_{n+1}}}}", two heads, from=1-3, to=1-4]
        \arrow["{{{b_{n+1}}}}", from=1-3, to=2-3]
        \arrow[from=1-4, to=1-5]
        \arrow["{{{c_{n+1}}}}", from=1-4, to=2-4]
        \arrow[from=2-1, to=2-2]
        \arrow["{{{f_n}}}", hook', from=2-2, to=2-3]
        \arrow["{{{a_n}}}", from=2-2, to=3-2]
        \arrow["{{{g_n}}}", two heads, from=2-3, to=2-4]
        \arrow["{{{b_n}}}", from=2-3, to=3-3]
        \arrow[from=2-4, to=2-5]
        \arrow["{{{c_{n}}}}", from=2-4, to=3-4]
        \arrow[from=3-1, to=3-2]
        \arrow["{{{f_{n-1}}}}", hook', from=3-2, to=3-3]
        \arrow["{a_{n-1}}", from=3-2, to=4-2]
        \arrow["{{{g_{n-1}}}}", two heads, from=3-3, to=3-4]
        \arrow["{b_{n-1}}", from=3-3, to=4-3]
        \arrow[from=3-4, to=3-5]
        \arrow["{c_{n-1}}", from=3-4, to=4-4]
        \arrow[from=4-1, to=4-2]
        \arrow["{f_{n-2}}", hook', from=4-2, to=4-3]
        \arrow["{g_{n-2}}", two heads, from=4-3, to=4-4]
        \arrow[from=4-4, to=4-5]
    \end{tikzcd}\]
    since we are dealing with chain complexes, it suffices to show $\ker(a_n)\subseteq\im(a_{n+1})$.
    Choose $x,y\in A_n$ such that $a_n(x)=a_n(y)$ and assume $x\neq y$ (otherwise we are done), then we have
    $b_n(f_n(x))=f_{n-1}(a_n(x))=f_{n-1}(a_n(y))=b_n(f_n(y))$ which implies 
    $(f_n(x),f_n(y))\in\im(b_{n+1})$, since $f_n(x)\neq f_n(y)$ there are 
    $x_1,y_1\in B_{n+1}$ such that $b_{n+1}(x_1)=f_n(x)$ and $b_{n+1}(y_1)=f_n(y)$.
    Then we have 
    \[
    c_{n+1}(g_{n+1}(x_1))=g_n(b_{n+1}(x_1))=g_n(f_n(x))=0=g_n(f_n(y))= g_n(b_{n+1}(y_1))=c_{n+1}(g_{n+1}(y_1))
    \]
    therefore $(g_{n+1}(x_1),g_{n+1}(y_1))\in\im(c_{n+2})$ by exactness.\par
    If $g_{n+1}(x_1)=g_{n+1}(y_1)$, then $(x_1,y_1)\in\im(f_{n+1})$, we know
    $x_1\neq y_1$, else $f_n(x)=f_n(y) \implies x=y$, a contradiction.
    So there are $x_2,y_2\in A_{n+1}$ such that 
    $f_{n+1}(x_2)=x_1$ and $f_{n+1}(y_2)=y_1$, but then 
    \[
        f_{n}(a_{n+1}(x_2)) = b_{n+1}(f_{n+1}(x_2)) = b_{n+1}(x_1) = f_n(x) \implies x=a_{n+1}(x_2)
    \] 
    \[
        f_{n}(a_{n+1}(y_2)) = b_{n+1}(f_{n+1}(y_2)) = b_{n+1}(y_1) = f_n(y) \implies y=a_{n+1}(y_2)
    \]
    therefore $(x,y)\in\im(a_{n+1})$ as required.\par
    If $g_{n+1}(x_1)\neq g_{n+1}(y_1)$, then since $(g_{n+1}(x_1),g_{n+1}(y_1))\in\im(c_{n+2})$ we find
    $z_1,w_1\in C_{n+2}$ such that $c_{n+2}(z_1)=g_{n+1}(x_1)$ and $c_{n+2}(w_1)=g_{n+1}(y_1)$. Since
    $g_{n+2}$ is an epimorphism we find $z_2,w_2\in B_{n+2}$ such that 
    $g_{n+2}(z_2)=z_1$ and $g_{n+2}(w_2)=w_1$. Now we have 
    \[
        g_{n+1}(b_{n+2}(z_2)) = c_{n+2}(g_{n+2}(z_2))=c_{n+2}(z_1)=g_{n+1}(x_1)
    \]
    \[
        g_{n+1}(b_{n+2}(w_2)) = c_{n+2}(g_{n+2}(w_2)) = c_{n+2}(w_1) = g_{n+1}(y_1)
    \]
    which implies that $(b_{n+2}(z_2),x_1)\in\im(f_{n+1})$ and $(b_{n+2}(w_2),y_1)\in\im(f_{n+1})$
    by exactness. Now assume that $x_1=b_{n+2}(z_2)$ then
    \[
        f_n(x)=b_{n+1}(x_1)= b_{n+1}(b_{n+2}(z_2)) = 0 = f_{n}(0) \implies x=0=a_{n+1}(0)
    \]
    by symmetry the same holds for $y$, but both cannot hold at the same time since we would have $x=y$ which would be a contradiction.
    Without loss of generality we will only consider the case when $x_1\neq b_{n+2}(z_2)$ and $y=0=a_{n+1}(0)$, then 
    since $(x_1,b_{n+2}(z_2))\in\im(f_{n+1})$ we find $x_2\in A_{n+1}$ such that 
    $f_{n+1}(x_2)=x_1$
    then 
    \[
        f_{n}(a_{n+1}(x_2)) = b_{n+1}(f_{n+1}(x_2)) = b_{n+1}(x_1) = f_n(x) \implies a_{n+1}(z_1)=x
    \]\par
    Now assume that $A$ and $B$ are exact. Choose $x,y\in\ker(c_n)$, $x\neq y$, find $x_1,y_1\in B_n$ such that 
    $g_n(x_1)=x$,$g_n(y_1)=y$, then $g_{n-1}(b_n(x_1))=c_n(x)=c_n(y)=g_{n-1}(b_n(y_1))$
    which implies $(b_n(x_1),b_n(y_1))\im f_{n-1}$.\par
    If $b_n(x_1)=b_n(y_1)$, then $(x_1,y_1)\in\im(b_{n+1})$ (note that 
    we have $x_1\neq y_1$, because $x\neq y$) so we find $b,c\in B_{n+1}$ 
    such that $b_{n+1}(b) = x_1$ and $b_{n+1}(c)=y_1$, then 
    \[
        x = g_n(x_1) = g_n(b_{n+1}(b)) = c_{n+1}(g_{n+1}(b))
    \] 
    \[
        y = g_n(y_1) = g_n(b_{n+1}(c)) = c_{n+1}(g_{n+1}(c)) 
    \]
    therefore $(x,y)\in\im(c_{n+1})$.\par
    If $b_n(x_1)\neq b_n(y_1)$, then since $(b_n(x_1),b_n(y_1))\in\im(f_{n-1})$
    we find $x_2,y_2\in A_{n-1}$ such that $f_{n-1}(x_2)=b_n(x_1)$ and 
    $f_{n-1}(y_2) = b_n(y_1)$. Now we compute
    \[
    f_{n-2}(a_{n-1}(x_2)) = b_{n-1}(b_n(x_1)) = 0 = b_{n-1}(b_n(y_1)) =
    f_{n-2}(a_{n-1}(y_2))
    \]
    hence $a_{n-1}(x_2)=a_{n-1}(y_2)$ and by exactness $(x_2,y_2)\in\im(a_n)$. 
    If $x_2=y_2$ we would have $b_n(x_1)=b_n(y_1)$ which contradicts our assumption 
    so we find $x_3,y_3\in A_n$ such that $a_n(x_3)=x_2$ and $a_n(y_3)=y_2$
    then $b_n(f_n(x_3)) = f_{n-1}(a_n(x_3)) = b_n(x_1)$ and similarly 
    $b_n(f_n(y_3)) = b_n(y_1)$. So by exactness $(f_n(x_3),x_1)\in\im(b_{n+1})$
    and $(f_n(y_3),y_1)\in\im(b_{n+1})$. From now on we will only consider $(x_1,f_n(x_3))$
    because the same reasoning applies to the other pair. If $f_n(x_3)=x_1$, then 
    $x=g_n(x_1) = g_n(f_n(x_3)) = 0 = c_{n+1}(0)$. If $f_n(x_3)\neq x_1$, then we find 
    $z_1\in B_{n+1}$ such that $x_1=b_{n+1}(z_1)$, then 
    \[
        x = g_n(x_1) = g_n(b_{n+1}(z_1)) = c_{n+1}(g_{n+1}(z_1))
    \]
    in either case $x$ in in the image of $c_{n+1}$ as required.\par 
    Finally assume that $A$ and $C$ are exact. Choose $(x,y)\in\ker(b_n)$,$x\neq y$, then 
    \[
    c_n(g_n(x)) = g_{n-1}(b_n(x)) = g_{n-1}(b_n(y)) = c_n(g_n(y)) \implies (g_n(x),g_n(y))\in\im(c_{n+1})
    \]
    If $g_n(x)=g_n(y)$, then $(x,y)\in\im(f_n)$, we find $x_1,y_1\in A_n$
    such that $f_n(x_1)=x$ and $f_n(y_1)=y$, then $x_1\neq y_1$ since $x\neq y$ and we have 
    \[
        f_{n-1}(a_n(x_1)) = b_n(f_n(x_1)) = b_n(x) = b_n(y) = f_{n-1}(a_n(y_1)) \implies a_n(x_1)=a_n(y_1)
    \]
    hence by exactness we find $x_2, y_2\in A_{n+1}$ such that 
    $a_{n+1}(x_2) = x_1$ and $A_{n+1}(y_2)=y_1$, then 
    $x=f_{n}(a_{n+1}(x_2)) = b_{n+1}(f_{n+1}(x_2))$ and similarly 
    $y = b_{n+1}(f_{n+1}(y_2))$, thus $(x,y)\in\im(b_{n+1})$. \par 
    If $g_{n}(x)\neq g_n(y)$, then we find $z_1,w_1\in C_{n+1}$ such that 
    $c_{n+1}(z_1)=g_n(x)$, $c_{n+1}(w_1)=g_n(y)$. Next we find $z_2,w_2\in B_{n+1}$
    such that $g_{n+1}(z_2) = z_1$, $g_{n+1}(w_2) = w_1$, then 
    $g_n(b_{n+1}(z_2)) = c_{n+1}(g_{n+1}(z_2)) = g_n(x)$ and 
    $g_n(b_{n+1}(w_2)) = c_{n+1}(g_{n+1}(w_2)) = g_n(y)$, thus 
    $(b_{n+1}(z_2),x)\in\im(f_n)$,$(b_{n+1}(w_2),y)\in\im(f_n)$.\par 
    If $x=b_{n+1}(z_2)$ and $y=b_{n+1}(w_2)$ we are done. Assume first that 
    $x\neq b_{n+1}(z_2)$ and $y=b_{n+1}(w_2)$. We find $r_1\in A_n$ such that 
    $f_n(r_1)=x$, then 
    \[
        f_{n-1}(a_n(r_1)) = b_n(f_{n}(r_1)) = b_n(x) = b_n(y) = b_n(b_{n+1}(w_2)) = 0 = f_{n-1}(0)
    \]
    The penultimate equality follows from the fact that $(B,b)$ is a chain complex. Since $f_{n-1}$
    is a monomorphism we have $a_n(r_1)=0=a_n(0)$, so $(r_1,0)\in\im(a_{n+1})$. If 
    $r_1=0$, then $x=0=b_{n+1}(0)$. Assume $r_1\neq 0$, then we find $r_2\in A_{n+1}$ such that 
    $a_{n+1}(r_2) =r_1$, then $x= f_{n}(a_{n+1}(r_2)) = b_{n+1}(f_{n+1}(r_2))$ 
    and we are done.\par 
    The case $x=b_{n+1}(z_2)$ and $y\neq b_{n+1}(w_2)$ resolves in the same way as the last one, by symmetry.\par 
    Assume $x\neq b_{n+1}(z_2)$ and $y \neq b_{n+1}(w_2)$, then we find $r_1,s_1\in A_n$ such that 
    $f_n(r_1)=x$,$f_n(s_1)=y$. We have 
    \[
    f_{n-1}(a_n(r_1))=b_n(f_n(r_1))=b_n(x)=b_n(y)=b_n(f_n(s_1))=
    f_{n-1}(a_n(s_1)) \implies a_n(r_1) = a_n(s_1)
    \]
    We cannot have $r_1=s_1$, since it would contradict our assumption $x\neq y$. So 
    we find $r_2,s_2\in A_{n+1}$ such that $a_{n+1}(r_2)=r_1$,$a_{n+1}(s_2)=s_1$, then 
    \[
        x = f_n(r_1) = f_n(a_{n+1}(r_2)) = b_{n+1}(f_{n+1}(r_2))
    \]
    \[
        y = f_n(s_1) = f_n(a_{n+1}(s_2)) = b_{n+1}(f_{n+1}(s_2))
    \]
    which concludes the proof.
\end{proof}
\begin{corollary}[3x3 lemma]
    Consider the following commutative diagram, whose rows are Rees exact
    \[\begin{tikzcd}
        & 0 & 0 & 0 \\
        0 & {A_3} & {B_3} & {C_3} & 0 \\
        0 & {A_2} & {B_2} & {C_2} & 0 \\
        0 & {A_1} & {B_1} & {C_1} & 0 \\
        & 0 & 0 & 0
        \arrow[from=1-2, to=2-2]
        \arrow[from=1-3, to=2-3]
        \arrow[from=1-4, to=2-4]
        \arrow[from=2-1, to=2-2]
        \arrow["{f_3}", from=2-2, to=2-3]
        \arrow["{r_3}", from=2-2, to=3-2]
        \arrow["{g_3}", from=2-3, to=2-4]
        \arrow["{r_2}", from=2-3, to=3-3]
        \arrow[from=2-4, to=2-5]
        \arrow["{r_1}", from=2-4, to=3-4]
        \arrow[from=3-1, to=3-2]
        \arrow["{f_2}", from=3-2, to=3-3]
        \arrow["{s_3}", from=3-2, to=4-2]
        \arrow["{g_2}", from=3-3, to=3-4]
        \arrow["{s_2}", from=3-3, to=4-3]
        \arrow[from=3-4, to=3-5]
        \arrow["{s_1}", from=3-4, to=4-4]
        \arrow[from=4-1, to=4-2]
        \arrow["{f_1}", from=4-2, to=4-3]
        \arrow[from=4-2, to=5-2]
        \arrow["{g_1}", from=4-3, to=4-4]
        \arrow[from=4-3, to=5-3]
        \arrow[from=4-4, to=4-5]
        \arrow[from=4-4, to=5-4]
    \end{tikzcd}\]
    \begin{enumerate}
        \item If the first and the second columns are exact, then so is the third.
        \item If the second and the third columns are exact, then so is the first.
        \item If the first and the third columns are exact and additionaly $s_2\circ r_2=0$, then so is the second.
    \end{enumerate}
\end{corollary}
\begin{proof}[Proof]
    For the first part we will have to show, that the sequence 
    \[\begin{tikzcd}
        \cdots & 0 & {C_3} & {C_2} & {C_1} & 0 & \cdots
        \arrow[from=1-1, to=1-2]
        \arrow[from=1-2, to=1-3]
        \arrow["{r_1}", from=1-3, to=1-4]
        \arrow["{s_1}", from=1-4, to=1-5]
        \arrow[from=1-5, to=1-6]
        \arrow[from=1-6, to=1-7]
    \end{tikzcd}\]
    is a chain complex. It suffices to prove that $\im(r_1)\subseteq\ker(s_1)$.
    We have to show $s_1\circ r_1 = 0$, since $g_3$ is epic we have 
    \[
        s_1\circ r_1\circ g_3 = s_1\circ g_2\circ r_2 = g_1\circ s_2\circ s_1 = g_1 \circ 0 = 0 = 0\circ g_3 \implies s_1\circ r_1 = 0
    \]
    Now applying the previous proposition to the evident chain complex yields the statement.\par
    Assume now that the second and the third columns are exact. We again only have to show that 
    $s_3\circ r_3 = 0$. We have 
    \[
    f_1\circ s_3\circ r_3 = s_2 \circ f_2 \circ r_3 = s_2 \circ r_2 \circ f_3 = 0\circ f_3 = 0 = f_1\circ 0 \implies s_3\circ r_3 = 0
    \]
    For the third part we can just directly apply the previous proposition. This completes the proof.
\end{proof}